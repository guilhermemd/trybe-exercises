const validateUsername = require('../services');
const validateEmail = require('../services');
const validatePassoword = require('../services');

module.exports = {
  validateUsername,
  validateEmail,
  validatePassoword,
};


